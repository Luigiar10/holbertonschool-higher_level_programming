#!/usr/bin/python3
"""Square based in 0-square.py"""


class Square:
       """inizialiation and display of messages"""
       def __init__(self, size=0):
              if zize != :
                     try:
                            
                     print("size must be an integer")
              else:
                     print("size must be >= 0 You are not allowed to import any module")
